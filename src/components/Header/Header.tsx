import styles from './Header.module.css';

const Header = () => {
  return (
    <header>
      <h1 className={styles.title}>Gram-Schmidt Calculator</h1>
    </header>
  );
};

export default Header;
